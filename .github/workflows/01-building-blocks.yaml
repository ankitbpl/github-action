name: 01 - Building Blocks
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo " hello, world"
    echo-goodby:
        runs-on: ubuntu-latest
        steps:
            - name: Successful step
              run: |
                echo " I will pass"
                exit 0
            - name: Say Goodby
              run: echo " Goodby!"
